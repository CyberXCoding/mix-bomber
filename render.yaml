services:
  - type: web
    name: sms-api-bomber
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 2 -k gevent --timeout 120 app:app"
    autoDeploy: false   # manual for control
